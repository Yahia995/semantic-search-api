services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data  # Persist FAISS index
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped

volumes:
  data:
